{
  "name": "webrtcwithserver",
  "version": "1.0.0",
  "description": "A simple WebRTC-based chat application that allows two users to connect and exchange messages in real-time using Firebase for signaling. The application generates a unique connection code for Peer A, which can be shared with Peer B for establishing a WebRTC data channel. The connection status and a QR code for easy sharing are also displayed.",
  "main": "config.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ws": "^8.18.0",
    "ejs": "^3.1.10",
    "express": "^4.21.1",
    "body-parser": "^1.20.3"
  }
}
